<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_bg"
    android:orientation="vertical" >

    <!-- <include layout="@layout/topbar" /> -->

    <!-- <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#fff"
        android:layout_gravity="center_horizontal"
        android:gravity="center" >

        <Spinner
            android:id="@+id/spSwitchCategory"
            android:layout_width="@dimen/widthSpinnerSwitchCategory"
            android:layout_height="@dimen/heightSpinnerperplate"
            android:spinnerMode="dropdown"
            android:background="#00000000" />
        <Button 
            android:id="@+id/btnSpinnerOpen"
            android:layout_height="@dimen/heightArrowDown"
            android:layout_width="@dimen/widthArrowDown"
            android:background="@drawable/arrow_down"/>
    </LinearLayout> -->

    <ListView
        android:id="@+id/favList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:horizontalSpacing="0dp"
        android:scrollbars="none"
        android:verticalSpacing="0dp" >

        <!-- tools:listitem="@layout/favorite_item" -->
    </ListView>

    <!-- <include layout="@layout/bottombar" /> -->

</LinearLayout>